//Prim's algorithm (also known as Jarník's algorithm) is a greedy algorithm that finds a minimum spanning tree for a weighted undirected graph.

class PrimsMaze3dGenerator {
  constructor(maze) {
    super();
    this.maze = maze;
  };
  generate() { };
}